# LaTeX build artifacts
cv.aux
cv.log
cv.out
cv.fls
cv.fdb_latexmk
cv.synctex.gz

# Job-market CV variants (repo-local applications material — the repo is
# public, so the PDFs must never be committed; rebuild with build:cv:jm)
cv_us.*
cv_eu.*

# Local preview / debug screenshots
preview*.png
live-*.png

# Astro / Node
node_modules/
dist/
.astro/
.env
.env.local

# Private — never commit to public webpage repo
scholarship_application_2026-2027/
.DS_Store
.playwright-mcp/
.superpowers/
